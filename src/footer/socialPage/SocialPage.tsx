import React from 'react'
import styles from './SocialPage.module.css'

export type SocialPagePropsType = {
    icon: string
}

export const SocialPage = (props: SocialPagePropsType) => {
    return (
        <div className={styles.socialPageBlock}>

        </div>
    )
}